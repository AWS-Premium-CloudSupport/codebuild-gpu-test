name: GPU Test Workflow
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on:
      - gpu-test-1-${{ github.run_id }}-${{ github.run_attempt }}
      - image:381492037974.dkr.ecr.us-east-1.amazonaws.com/codebuild-cuda12:latest
      - instance-size:gpu_large
      - buildspec-override:true
    steps:
      - uses: actions/checkout@v3
      - name: Run GPU Tests
        run: |
          echo "This is handled by CodeBuild. See buildspec.yml for execution."
